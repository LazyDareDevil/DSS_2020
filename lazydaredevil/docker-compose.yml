version: '3.4'

networks:
  vk-crawler_crawler_internal:
    external: true

services:
  lazydaredevil:
    image: lazydaredevil
    container_name: lazydaredevil
    ports:
      - "5000:5000"
    build:
      context: .
      dockerfile: ./Dockerfile
    networks:
      - vk-crawler_crawler_internal
